export default class Category {
    readonly blogingID: string;
    name: string;
    description: string;
    postID: number;
    constructor(blogingID: string, name: string, description: string, postID: number);
}
